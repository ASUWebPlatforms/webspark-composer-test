/**
 * @file
 * storify behaviors.
 */
(function (Drupal) {

  'use strict';

  Drupal.behaviors.storify = {
    attach: function (context, settings) {

      console.log('It works!');

    }
  };

} (Drupal));
