{% set classes = [] %}
{% if ( bundle == 'article' ) %}
  {% set classes = classes|merge(['card-title']) %}
{% endif %}
{% if heading|length > 4 %}
  {% set classes = classes|merge(['card-title']) %}
{% endif %}

<{{ html_tag }}{{ classes|length > 0 ? attributes.addClass(classes) }} tabindex="0">
  {{ heading }}
</{{ html_tag }}>
