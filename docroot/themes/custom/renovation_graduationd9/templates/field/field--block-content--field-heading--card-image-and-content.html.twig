{% for item in items %}
  <div{{ attributes.addClass(classes) }}>
    {% include '@renovation_graduationd9/headings/heading.twig' with {
      html_tag : 'h2',
      heading: item.content['#context'].value,
    } %}
  </div>
{% endfor %}
