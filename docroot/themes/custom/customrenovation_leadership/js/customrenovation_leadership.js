/**
 * @file
 * customrenovation_leadership behaviors.
 */

(function (Drupal, $, once) {

  'use strict';

  /**
   * Behavior description.
   */
  Drupal.behaviors.customrenovation_leadership = {
    attach: function (context, settings) {



    }
  };

} (Drupal, jQuery, once));
