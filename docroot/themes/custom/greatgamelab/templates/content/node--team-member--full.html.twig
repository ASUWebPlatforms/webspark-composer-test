{#
/**
 * @file
 * Custom Template for a Blog Article Node component.
 */
#}

{%
  set classes = [
  'node',
  node.bundle|clean_class,
  node.bundle|clean_class ~ '--' ~ view_mode|clean_class,
  'spacing-top-96',
  'spacing-bottom-96',
]
%}
<div class="layout__fixed-width">
    <div class="max-size-container center-container bg-white">
        <div class="container">
            <div class="row">
                <div class="col-12">
                    <article {{ attributes.addClass(classes) }} >
                    {{ title_prefix }}
                    {{ title_suffix }}
                    {% block content %}
                        <div class="ggl-article-image mb-5 mx-auto">
                            {{ content.field_media|render }}
                        </div>
                        <div class="ggl-article-title mx-auto">
                          <h1>{{ node.label }}</h2>
                        </div>
                        <div class="ggl-article-subhead lead-md mx-auto">
                          
                          {{ content.field_teammember_title|render }}
                          {% if content.field_team_member_city is not empty %}{{ content.field_team_member_city|render|striptags|trim }}{% endif %}
                        </div>

                        <div class="ggl-article-body set-piece-body mx-auto">

                            <div class="ggl-article-body__article_content mt-4 mb-6">
                                {{ content.body }}
                            </div>
                            <p class="mt-4"><a href="/about/team">Back to Team</a></p>
                        </div>
                    {% endblock %}
                    </article>
                </div>
            </div>
        </div>
    </div>
</div>
