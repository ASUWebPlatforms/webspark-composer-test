{% set divider = '' %}
{% if node.field_group.entity.label.value %}
  {% set divider = ' | ' ~ node.field_group.entity.label.value %}
{% endif %}
{% set data = {
  'title': node.title.value,
  'content': 'Tableau Cards' ~ divider,
  'button': {
    'url': node|entity_url,
    'color': 'gold',
    'text': 'View dashboard',
  }
} %}

<article class="tableau_cards__teaser--minimal">
  {{ include('@analytics/teaser-minimal.html.twig', data) }}
</article>
