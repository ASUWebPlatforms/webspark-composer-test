{% set data = {
  'title': node.title.value,
  'content': node.type.entity.label|title,
  'button': {
    'url': node|entity_url,
    'color': 'gold',
    'text': 'View report',
  }
} %}

<article class="report__teaser--minimal">
  {{ include('@analytics/teaser-minimal.html.twig', data) }}
</article>
