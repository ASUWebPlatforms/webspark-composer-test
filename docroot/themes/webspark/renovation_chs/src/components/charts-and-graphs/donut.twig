{{ attach_library('renovation_chs/renovation_chs.chartjs') }}
{{ attach_library('renovation_chs/charts_graphs') }}

<div class="uds-charts-and-graphs-container" data-number="{{ number }}">
  <div class="uds-charts-and-graphs-overlay {{ text_color }}">
    <p>
      <span id="percentage-display">{{ number }}%</span>
      <span>{{ text }}</span>
    </p>
  </div>
  <canvas id="uds-donut"></canvas>
</div>
