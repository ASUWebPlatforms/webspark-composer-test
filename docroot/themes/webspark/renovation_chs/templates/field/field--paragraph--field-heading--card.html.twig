{#
/**
 * @file
 * Default template for a field.
 */
#}

{% for item in items %}
  {# Heading #}

    {% include '@renovation_chs/headings/card-heading.twig' with {
      html_tag : 'h3',
      heading: item.content['#context'].value,
    } %}

{% endfor %}

