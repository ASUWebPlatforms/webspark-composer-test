{% set classes = [dom_id ? 'js-view-dom-id-' ~ dom_id] %}
{% if header %}
  {{ header }}
{% endif %}

<div {{ attributes.addClass(classes) }}>
  <div class="container">
    <div class="row">
      {{ rows }}
    </div>
    {{ pager }}
  </div>
</div>
