# Module Documentation

This custom module enables some mild CSS and template edits/overrides. The ghost block must be enabled and added to the site as a block for overrides to work. 

# Updates

2.1.0
- Moved styles and edits from CHS custom theme to this module. Changes in Webspark 2.11 broke some card styles in the custom theme.
- Removed MailChimp functionality